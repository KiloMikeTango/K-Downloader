// 1. ENUMS & CONSTANTS (models/enums.dart)

enum TransferPhase { idle, downloading, extracting, uploading }

enum DownloadMode { video, audio, both }

enum LinkType { youtube, facebook, tiktok, invalid }
